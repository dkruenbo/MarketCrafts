# WoW SavedVariables (generated at runtime, not source)
*.bak
WTF/

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor
.vscode/
*.swp
*.swo

# The .gitkeep placeholders can be removed once real lib files are present
# but we keep them so the directory structure is visible in the repo
